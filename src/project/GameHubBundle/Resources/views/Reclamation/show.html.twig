
{% extends 'base.html.twig' %}

{% block body %}
    <div class="wrap-content container" id="container">
        <!-- start: DASHBOARD TITLE -->
        <section id="page-title"  >
            <div class="row">
                <div class="col-sm-7">
                    <h1 class="mainTitle">Liste des reclamtions de <strong> {{ Reclamation[0].pseudo }}</strong></h1>
                </div>
                <ol class="breadcrumb">
                    <li>
                        <span>Reclamtions</span>
                    </li>
                    <li class="active">
                        <span>Liste des reclamtions   </span>
                    </li>
                </ol>
            </div>
        </section>
        <!-- end: DASHBOARD TITLE -->
        <!-- start: UI BOOTSTRAP MODALS -->
        <div class="container-fluid container-fullw">
            <div class="row">
                <div class="col-md-12">
                    <div class="row">
                        <div class="col-md-12 space20">

                        </div>
                    </div>
                    <a class="table-responsive">
                        <table class="table table-striped table-hover" id="sample_2">
                            <thead>
                            <tr>
                                <th>description</th>
                                <th>crée par</th>
                                <th>url</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for entity in Reclamation %}
                                <tr>
                                    <td>{{ entity.description }}</td>
                                    <td>{{ entity.idM }}</td>
                                    <td><a href="{{ asset('uploads/urls/' ~ entity.url) }}"><img src="{{ asset('uploads/urls/' ~ entity.url) }}" width="30" height="30"> </a> </td>

                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

    </div>

    <!-- end: UI BOOTSTRAP MODALS -->


{% endblock %}
